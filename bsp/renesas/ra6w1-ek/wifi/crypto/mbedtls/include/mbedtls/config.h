/* Keep the generated FSP configuration at the include path expected by
 * MbedTLS. This shim is local to the Wi-Fi build and does not alter the
 * RT-Thread or system-wide include paths. */
#include "../../../../config/mbedtls/config.h"
